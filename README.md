# medical-doc-versioning-api
Versioned document-to-QA pipeline: parses a manual into a structured tree, tracks changes across versions, and flags AI-generated test cases as stale when source text changes.
